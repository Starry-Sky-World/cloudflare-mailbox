{
  "name": "cloudflare-mailbox",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute mailbox-db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.5.2",
    "jose": "^5.6.3",
    "postal-mime": "^2.2.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240806.0",
    "typescript": "^5.6.2",
    "wrangler": "^4.54.0"
  }
}
